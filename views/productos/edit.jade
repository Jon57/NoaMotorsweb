extends ../layout.jade
block contenido
		div(class="col-md-3 center-block quitar-float top-space text-left")
			h1 Editar Producto
			form(method="POST" action="/productos/#{product.id}?_method=PUT" style="margin-top:1.5em;" enctype="multipart/form-data")

				div(class="form-group")
					label Nombre
					input(type="text" name="title" value="#{product.title}" placeholder="Nombre de Producto" class="form-control")
				div(class="form-group")
					label Descripción
					textarea(type="text" name="description" placeholder="Descripción del producto" class="form-control")
						=product.description
				div(class="form-group")
					label costo
					input(type="number" name="pricing" placeholder="Precio" class="form-control" value="#{product.pricing}")
				div(class="form-group")
					label Imagen
					input(type="file" name="image_avatar")
				div(class="form-group")
					label Contraseña
					input(type="password" name="password" placeholder="Contraseña" class="form-control")
				div(class="form-group text-right")
					input(type="submit" class="btn btn-info" value="Guardar")